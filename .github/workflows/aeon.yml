name: Aeon

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
    inputs:
      skill:
        description: 'Skill to run (e.g. article, digest)'
        required: true
        type: string
  workflow_call:
    inputs:
      skill:
        description: 'Skill to run'
        required: true
        type: string
  repository_dispatch:
    types: [telegram-message, discord-message, slack-message]
  issues:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write
  issues: read

concurrency:
  group: aeon-main
  cancel-in-progress: false

jobs:
  run:
    if: github.event_name != 'issues' || github.event.label.name == 'ai-build'
    runs-on: ubuntu-latest

    steps:
      - name: Check messages
        id: messages
        if: github.event_name == 'schedule' || github.event_name == 'repository_dispatch'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
        run: |
          # --- Webhook trigger (repository_dispatch) ---
          if [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            MESSAGE=$(echo '${{ toJson(github.event.client_payload.message) }}' | jq -r '.')
            if [ -n "$MESSAGE" ] && [ "$MESSAGE" != "null" ]; then
              TYPE="${{ github.event.action }}"
              SOURCE="webhook"
              case "$TYPE" in
                telegram-message) SOURCE="telegram" ;;
                discord-message) SOURCE="discord" ;;
                slack-message) SOURCE="slack" ;;
              esac
              echo "source=$SOURCE" >> "$GITHUB_OUTPUT"
              echo "message<<ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
              echo "$MESSAGE" >> "$GITHUB_OUTPUT"
              echo "ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
            fi
            exit 0
          fi

          # --- Telegram (poll with offset-based ack) ---
          if [ -n "${TELEGRAM_BOT_TOKEN:-}" ] && [ -n "${TELEGRAM_CHAT_ID:-}" ]; then
            RESPONSE=$(curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates?timeout=0&allowed_updates=%5B%22message%22%5D")
            MESSAGE=$(echo "$RESPONSE" | jq -r '.result[-1].message.text // empty')
            UPDATE_ID=$(echo "$RESPONSE" | jq -r '.result[-1].update_id // empty')
            CHAT_ID=$(echo "$RESPONSE" | jq -r '.result[-1].message.chat.id // empty')

            if [ -n "$MESSAGE" ]; then
              if [ "$CHAT_ID" != "$TELEGRAM_CHAT_ID" ]; then
                echo "Telegram: ignoring message from unauthorized chat"
                curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates?offset=$((UPDATE_ID + 1))" > /dev/null
              else
                # Acknowledge
                curl -s "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates?offset=$((UPDATE_ID + 1))" > /dev/null
                echo "Telegram: $MESSAGE"
                echo "source=telegram" >> "$GITHUB_OUTPUT"
                echo "message<<ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                echo "$MESSAGE" >> "$GITHUB_OUTPUT"
                echo "ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                exit 0
              fi
            fi
          fi

          # --- Discord (poll channel, reaction-based ack) ---
          if [ -n "${DISCORD_BOT_TOKEN:-}" ] && [ -n "${DISCORD_CHANNEL_ID:-}" ]; then
            RESPONSE=$(curl -s -H "Authorization: Bot $DISCORD_BOT_TOKEN" \
              "https://discord.com/api/v10/channels/$DISCORD_CHANNEL_ID/messages?limit=1")
            MSG_CONTENT=$(echo "$RESPONSE" | jq -r '.[0].content // empty')
            MSG_ID=$(echo "$RESPONSE" | jq -r '.[0].id // empty')
            MSG_AUTHOR_BOT=$(echo "$RESPONSE" | jq -r '.[0].author.bot // false')

            if [ -n "$MSG_CONTENT" ] && [ "$MSG_AUTHOR_BOT" != "true" ]; then
              # Check if already processed (bot reacted with checkmark)
              HAS_REACTION=$(echo "$RESPONSE" | jq -r '.[0].reactions[]? | select(.emoji.name == "✅") | .me // false')
              if [ "$HAS_REACTION" != "true" ]; then
                # Mark as processed
                curl -s -X PUT -H "Authorization: Bot $DISCORD_BOT_TOKEN" \
                  "https://discord.com/api/v10/channels/$DISCORD_CHANNEL_ID/messages/$MSG_ID/reactions/%E2%9C%85/@me" > /dev/null
                echo "Discord: $MSG_CONTENT"
                echo "source=discord" >> "$GITHUB_OUTPUT"
                echo "message<<ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                echo "$MSG_CONTENT" >> "$GITHUB_OUTPUT"
                echo "ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                exit 0
              fi
            fi
          fi

          # --- Slack (poll channel, reaction-based ack) ---
          if [ -n "${SLACK_BOT_TOKEN:-}" ] && [ -n "${SLACK_CHANNEL_ID:-}" ]; then
            RESPONSE=$(curl -s -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
              "https://slack.com/api/conversations.history?channel=$SLACK_CHANNEL_ID&limit=1")
            MSG_TEXT=$(echo "$RESPONSE" | jq -r '.messages[0].text // empty')
            MSG_TS=$(echo "$RESPONSE" | jq -r '.messages[0].ts // empty')
            MSG_BOT_ID=$(echo "$RESPONSE" | jq -r '.messages[0].bot_id // empty')

            if [ -n "$MSG_TEXT" ] && [ "$MSG_BOT_ID" = "null" ]; then
              # Check if already processed
              HAS_REACTION=$(echo "$RESPONSE" | jq -r '.messages[0].reactions[]? | select(.name == "white_check_mark") | .name // empty')
              if [ -z "$HAS_REACTION" ]; then
                # Mark as processed
                curl -s -X POST -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
                  -H "Content-Type: application/json" \
                  -d "$(jq -n --arg ch "$SLACK_CHANNEL_ID" --arg ts "$MSG_TS" \
                    '{channel: $ch, timestamp: $ts, name: "white_check_mark"}')" \
                  "https://slack.com/api/reactions.add" > /dev/null
                echo "Slack: $MSG_TEXT"
                echo "source=slack" >> "$GITHUB_OUTPUT"
                echo "message<<ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                echo "$MSG_TEXT" >> "$GITHUB_OUTPUT"
                echo "ENDOFMESSAGE" >> "$GITHUB_OUTPUT"
                exit 0
              fi
            fi
          fi

          echo "No new messages"

      - name: Determine skill
        id: skill
        if: steps.messages.outputs.source == ''
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] || [ "${{ github.event_name }}" = "workflow_call" ]; then
            echo "name=${{ inputs.skill }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "issues" ]; then
            echo "name=feature" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            MINUTE=$(date -u +%-M)
            HOUR=$(date -u +%-H)
            DOM=$(date -u +%-d)
            MONTH=$(date -u +%-m)
            DOW=$(date -u +%w)

            cron_match() {
              local field="$1" value="$2"
              [ "$field" = "*" ] && return 0
              if [[ "$field" == */* ]]; then
                local interval="${field#*/}"
                [ $((value % interval)) -eq 0 ] && return 0
                return 1
              fi
              IFS=',' read -ra VALS <<< "$field"
              for v in "${VALS[@]}"; do
                [ "$v" = "$value" ] && return 0
              done
              return 1
            }

            declare -A SKILL_ENABLED SKILL_SCHEDULE
            SKILL_ORDER=()
            CURRENT_SKILL=""
            while IFS= read -r line; do
              if [[ "$line" =~ ^\ \ ([a-zA-Z0-9_-]+):\ *$ ]]; then
                CURRENT_SKILL="${BASH_REMATCH[1]}"
                SKILL_ORDER+=("$CURRENT_SKILL")
                SKILL_ENABLED["$CURRENT_SKILL"]="true"
                SKILL_SCHEDULE["$CURRENT_SKILL"]=""
              fi
              if [[ "$line" =~ ^\ \ \ \ enabled:\ *false ]] && [ -n "$CURRENT_SKILL" ]; then
                SKILL_ENABLED["$CURRENT_SKILL"]="false"
              fi
              if [[ "$line" =~ ^\ \ \ \ schedule:\ *\"([^\"]+)\" ]] && [ -n "$CURRENT_SKILL" ]; then
                SKILL_SCHEDULE["$CURRENT_SKILL"]="${BASH_REMATCH[1]}"
              fi
            done < aeon.yml

            for SKILL in "${SKILL_ORDER[@]}"; do
              [ "${SKILL_ENABLED[$SKILL]}" = "false" ] && continue
              SCHED="${SKILL_SCHEDULE[$SKILL]}"
              [ -z "$SCHED" ] && continue

              IFS=' ' read -r C_MIN C_HOUR C_DOM C_MONTH C_DOW <<< "$SCHED"
              cron_match "$C_MIN" "$MINUTE" || continue
              cron_match "$C_HOUR" "$HOUR" || continue
              cron_match "$C_DOM" "$DOM" || continue
              cron_match "$C_MONTH" "$MONTH" || continue
              cron_match "$C_DOW" "$DOW" || continue
              echo "name=$SKILL" >> "$GITHUB_OUTPUT"
              echo "Matched skill: $SKILL"
              break
            done
          fi

      - name: Check if there's work
        id: work
        run: |
          if [ -n "${{ steps.messages.outputs.source }}" ]; then
            echo "mode=message" >> "$GITHUB_OUTPUT"
            echo "label=${{ steps.messages.outputs.source }}" >> "$GITHUB_OUTPUT"
          elif [ -n "${{ steps.skill.outputs.name }}" ]; then
            echo "mode=skill" >> "$GITHUB_OUTPUT"
            echo "label=${{ steps.skill.outputs.name }}" >> "$GITHUB_OUTPUT"
          else
            echo "Nothing to do"
          fi

      - name: Checkout repo
        if: steps.work.outputs.mode != ''
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        if: steps.work.outputs.mode != ''
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Claude Code
        if: steps.work.outputs.mode != ''
        run: npm install -g @anthropic-ai/claude-code

      - name: Run
        if: steps.work.outputs.mode != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
          COINGECKO_API_KEY: ${{ secrets.COINGECKO_API_KEY }}
          ALCHEMY_API_KEY: ${{ secrets.ALCHEMY_API_KEY }}
        run: |
          TODAY=$(date -u +%Y-%m-%d)
          ALLOWED="Read,Write,Edit,Glob,Grep,WebFetch,WebSearch"
          ALLOWED="$ALLOWED,Bash(curl:*),Bash(gh:*),Bash(git:*),Bash(jq:*)"
          ALLOWED="$ALLOWED,Bash(bash notify.sh:*),Bash(./notify.sh:*)"
          ALLOWED="$ALLOWED,Bash(mkdir:*),Bash(ls:*),Bash(cat:*),Bash(chmod:*)"
          ALLOWED="$ALLOWED,Bash(date:*),Bash(echo:*),Bash(node:*),Bash(npm:*),Bash(npx:*)"
          ALLOWED="$ALLOWED,Bash(head:*),Bash(tail:*),Bash(wc:*),Bash(sort:*),Bash(grep:*)"

          if [ "${{ steps.work.outputs.mode }}" = "message" ]; then
            SOURCE="${{ steps.messages.outputs.source }}"
            MESSAGE="${{ steps.messages.outputs.message }}"
            claude -p "Today is $TODAY. You received this message from the user on ${SOURCE}:

            \"$MESSAGE\"

            Read memory/MEMORY.md for context. Look at the available skills in skills/ to understand what you can do.

            Interpret what the user wants and act on it:
            - If they want you to run a skill (e.g. \"write an article about X\", \"send me a digest\"), read that skill file and execute it.
            - If they want information or have a question, research it and respond.
            - If they want you to remember something, update memory/MEMORY.md.
            - For anything else, do your best and explain what you did.

            Always send your response back via notify.sh so the user sees it." \
              --allowedTools "$ALLOWED"
          else
            SKILL_NAME="${{ steps.skill.outputs.name }}"
            claude -p "Today is $TODAY. Read and execute the skill defined in skills/${SKILL_NAME}/SKILL.md" \
              --allowedTools "$ALLOWED"
          fi

      - name: Commit results
        if: steps.work.outputs.mode != ''
        run: |
          git config user.email "aeon@github-actions"
          git config user.name "Aeon"
          git add -A
          git diff --staged --quiet && exit 0
          LABEL="${{ steps.work.outputs.label }}"
          git commit -m "chore($LABEL): auto-commit $(date +%Y-%m-%d)"
          for i in 1 2 3; do
            git pull --rebase && git push -u origin HEAD && exit 0
            echo "Push attempt $i failed, retrying..."
          done
          echo "Failed to push after 3 attempts"
          exit 1
